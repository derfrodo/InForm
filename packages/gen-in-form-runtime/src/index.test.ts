describe("runtime index tests", () => {
    beforeEach(() => {
        jest.clearAllMocks();
    });

    it("First test", async () => {
        // well i am actually empty - there should not be so much to test for index.js, right?

        expect(1).toBe(1);
    });
});
